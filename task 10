Find at least three compile-time errors:

int x = 2 <- We should not put int
Print (x, squared is, x * x) <- because we have to put quotes
xcubed = x *** 3 <- because we can’t put three * , we can do like this x*x*x
